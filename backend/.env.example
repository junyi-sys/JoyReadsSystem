# ===== 用户配置（仅放密钥，环境无关的配置放在 .env.dev / .env.prod） =====
# 复制此文件为 .env 并填入真实值

# 数据库（连接信息和 DB_NAME 由 .env.dev/.env.prod 管理，这里只填密码）
DB_PASSWORD=

# LLM
DEEPSEEK_API_KEY=sk-xxx

# 语音识别 STT（本地 faster-whisper，无需 API key，可选 tiny/base/small/medium）
STT_MODEL=base

# 图片生成
GLM_API_KEY=xxx
